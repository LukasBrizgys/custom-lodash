/**
 * Function takes array and element as parameter,
 * element is pushed into given array
 * @param {Array} array Array in which element will be pushed
 * @param {*} element 
 * @returns 
 */
const push = (array, element) => [...array, element];

export default push;